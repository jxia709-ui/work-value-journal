# Source fixes verified by GitHub Actions before production deploy.
[build]
  command = "npx next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
